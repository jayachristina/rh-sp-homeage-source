Run following command to deploy the html files in the `dist` folder to the git repo. this will trigger `gh-pages` as well
```
ng deploy --base-href=https://github.com/jayachristina/rh-patterns.git
```